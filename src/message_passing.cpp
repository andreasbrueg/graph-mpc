#include "message_passing.h"

void mp::scatter(ProtocolConfig &conf, SecretSharedGraph &graph) {}

void mp::gather(ProtocolConfig &conf, SecretSharedGraph &graph) {}

void mp::apply(ProtocolConfig &conf, SecretSharedGraph &graph) {}