#pragma once

#include <cstdint>
#include <iostream>
#include <vector>

enum Party { P0 = 0, P1 = 1, D = 2 };
enum Execution { BENCHMARK, TEST };

typedef uint32_t Ring;

typedef size_t wire_id;

typedef size_t SIMD_wire_id;

typedef size_t wire_id;